$<imports> 
import {{pascalCase name}}DTO from './dto/{{kebabCase name}}.dto';$<code>$<return> 
        const data = await {{camelCase name}}Controller.save(
            {{pascalCase name}}DTO.fromBody(event.body),
        );
        return new ApiGatewayResponse(
            '{{pascalCase name}} creado',
            StatusCodes.OK,
            data,
        );