// This file is only used for imports, not for creating a singleton instance
// The actual instance is created in the provider files

export { Logger } from './logger';
export { ILogger, LogLevel } from './logger.interface';
