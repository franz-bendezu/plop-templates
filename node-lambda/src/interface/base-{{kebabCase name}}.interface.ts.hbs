export default interface IBase{{pascalCase name}} {
    name: string;
    description: string;
    validFrom: Date;
    validTo: Date | null;
    {{#each paramsProperties}}
    {{name}}: {{type}};
    {{/each}}
}
