import IAudit from './audit.interface';
import IBase{{pascalCase name}} from './base-{{kebabCase name}}.interface';

export default interface I{{pascalCase name}} extends IAudit, IBase{{pascalCase name}} {
    id: number;
}
