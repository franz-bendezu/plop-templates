$<imports>
import Base{{pascalCase name}}DTO from '../dto/base-{{dashCase name}}.dto';$<code>$<methods>
    async save(data:  Base{{pascalCase name}}DTO): Promise<{{pascalCase name}}DTO> {
        const result = await this.{{camelCase name}}Service.save(data);
        return {{pascalCase name}}DTO.from(result);
    }