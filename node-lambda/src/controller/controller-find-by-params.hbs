$<imports>
import {{pascalCase name}}ParamsDTO from '../dto/{{kebabCase name}}-params.dto';$<code>$<methods>
    async findAllByParams(params: {{pascalCase name}}ParamsDTO): Promise<{{pascalCase name}}DTO[]> {
        const result = await this.{{camelCase name}}Service.findAllByParams(params);
        return result.map((conditionProduct) => {
            return {{pascalCase name}}DTO.from(conditionProduct);
        });
    }