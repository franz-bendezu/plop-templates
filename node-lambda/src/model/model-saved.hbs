import { TimeFormatter } from '../common/formatter/time.formatter';
import I{{pascalCase name}} from '../interface/{{kebabCase name}}.interface';
import { Audit } from './audit.model';
import Base{{pascalCase name}} from './base-{{kebabCase name}}.model';
import I{{pascalCase name}} from '../interface/{{kebabCase name}}.interface';

export default class {{pascalCase name}} extends Base{{pascalCase name}} implements I{{pascalCase name}} {

    constructor(
        public id: number,
        public name: string,
        public description: string,
        public validFrom: Date,
        public validTo: Date,
        public createdAt: Date | null,
        public modifiedAt: Date | null,
        public createdBy: string,
        public modifiedBy: string | null,
        {{#each modelProperties}}
        public {{name}}: {{type}},
        {{/each}}
    ) {
        super(name, description, validFrom, vali);
    }

    static from(data: I{{pascalCase name}}): {{pascalCase name}} {
        // Transform data into 
        return new {{pascalCase name}}(
            data.id,
            data.name,
            data.description,
            data.validFrom,
            data.validTo,
            data.createdAt,
            data.modifiedAt,
            data.createdBy,
            data.modifiedBy,
            {{#each modelProperties}}
            data.{{name}},
            {{/each}}
        );
    }

    // Methods
}