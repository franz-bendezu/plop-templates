// Imports
import { RuntimeError } from '../common/error/runtime-error';
import { StatusCodes } from '../common/response/status-code';
import { {{camelCase name}}Schema } from '../common/schema/{{kebabCase name}}.schema';

export interface IBase{{pascalCase name}}DTO {
    name: string;
    description: string;
    validFromDate: string | Date;
    validToDate?: string | Date | null;
}

export interface I{{pascalCase name}}Payload extends IBase{{pascalCase name}}DTO {
    validFromDate: string;
    validToDate?: string | null;
}

export default class Base{{pascalCase name}}DTO {

    constructor(
        public name: string,
        public description: string,
        public validFromDate: string,
        public validToDate?: string | null,
        {{#each modelProperties}}
        public {{name}}: {{type}},
        {{/each}}
    ) { }

    // Methods
}